# AGENTS.md

이 문서는 이 프로젝트에서 작업하는 모든 AI 에이전트(Claude Code 등)와 개발자가 따라야 할 개발 원칙을 정의합니다.

## 개발 원칙

1. 현재 기술 스택(Next.js, React, TypeScript, Python, FastAPI, pandas, NumPy, PostgreSQL, pgvector, n8n, LLM API)을 임의로 변경하지 않는다.
2. 새로운 라이브러리나 서비스를 임의로 추가하지 않는다.
3. 구현 전에 현재 단계의 목적을 확인한다.
4. 사용자가 명시하지 않은 기능을 임의로 구현하지 않는다.
5. 실제 존재하지 않는 데이터, API, 컬럼, 통계를 만들어내지 않는다.
6. 데이터 출처를 항상 추적할 수 있도록 한다.
7. 정량 데이터 계산은 Python/PostgreSQL에서 수행한다.
8. RAG는 근거 검색에 사용하고, LLM에게 숫자 계산을 맡기지 않는다.
9. n8n은 데이터 수집/ETL 자동화에 사용하고 핵심 비즈니스 로직은 FastAPI에서 관리한다.
10. MVP는 단순하게 유지하며 불필요한 추상화나 엔터프라이즈 아키텍처를 추가하지 않는다.
11. 한 번에 큰 기능을 구현하지 않고 작은 단위로 진행한다.
12. 사용자가 이해할 수 있도록 구현 이유와 변경 내용을 설명한다.
13. 기존 파일을 수정하기 전에 현재 구조와 내용을 확인한다.
14. 데이터베이스 스키마를 확정하기 전 실제 원본 데이터의 구조를 확인한다.
15. 실제 DB 생성이나 외부 API 연결은 사용자가 명시적으로 요청한 경우에만 수행한다.
16. 사용자가 "다음에 무엇을 해야 하는지", "지금 무엇을 해야 하는지", "다음 단계가 무엇인지" 등을 질문하면, 현재 프로젝트 상태를 먼저 확인한 뒤 가장 적절한 다음 작업을 제안한다.

17. 사용자가 직접 수행해야 하는 작업과 AI 에이전트가 수행할 작업을 구분해서 설명한다.

18. 다음 단계로 넘어가기 위해 필요한 사전 조건이 충족되지 않았다면, 다음 단계를 실행하지 말고 먼저 필요한 준비 작업을 안내한다.

19. 프로젝트 전체 로드맵에서 현재 위치를 명확하게 표시한다.
    예:
    [완료] 프로젝트 구조
    [완료] 개발환경
    [진행 중] 데이터 확보
    [대기] 데이터 분석
    [대기] DB 구축

20. 사용자가 초보자이므로 한 번에 너무 많은 작업을 요구하지 않고, 현재 단계에서 사용자가 직접 해야 할 일을 실행 가능한 작은 단위로 나누어 안내한다.

21. 작업을 실행하기 전에 사용자가 직접 확인하거나 결정해야 하는 사항이 있다면 그것을 먼저 알려준다.

22. 프로젝트의 전체 목표와 현재 단계의 목적을 연결해서 설명한다.단순히 "다음 명령어를 실행하라"고 하지 말고,
해당 작업이 서비스의 어느 부분을 만드는 과정인지 설명한다.

23. 사용자가 "앞으로 뭐 해야 해?"라고 질문하면, 전체 로드맵을 다시 장황하게 설명하지 말고 현재 상태를 기준으로 "지금 할 일 → 완료 조건 → 다음 단계" 순서로 안내한다.